// Initialize Materialize Components
$(document).ready(function () {
  // $('.sidenav').sidenav();
  
  // $('.slider').slider();
  // $('.carousel').carousel();

  $('.carousel.carousel-slider').carousel({
    fullWidth: true
  });
});